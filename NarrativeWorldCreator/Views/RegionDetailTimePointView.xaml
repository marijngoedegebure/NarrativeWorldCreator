<UserControl x:Class="NarrativeWorldCreator.Views.RegionDetailTimePointView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NarrativeWorldCreator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Grid.Row="0" Margin="10">
            <WrapPanel>
                <TextBlock Text="{Binding NarrativeTimePoint.TimePoint}" FontWeight="Bold" />
                <TextBlock Text="{Binding NarrativeTimePoint.NarrativeEvent.NarrativeAction.Name}" FontWeight="Bold" />
            </WrapPanel>
        </Grid>
        <ItemsControl
            Grid.Column="0"
            Grid.Row="1"
            Margin="10"
            Name="icTimePointList" 
            ItemsSource = "{Binding Path = NarrativeCharactersForSelectedNode}"
            ScrollViewer.VerticalScrollBarVisibility="Visible">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"></StackPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Name="item" Orientation="Vertical" Margin="5">
                        <TextBlock Text="{Binding Path = Name, Mode = OneWay}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Path = Placed, Mode = OneWay}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
